@model IEnumerable<ExampleMVC.Models.Example>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Description
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExampleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExampleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExampleId })
        </td>
        <td>
			@item.Description
        </td>
    </tr>
}

</table>


